# reporting-service

Internal financial reporting service. Flask + SQLite.

> **This repository is intentionally vulnerable.** It is a controlled test
> instrument for evaluating SAST/SCA/secrets scanners. Do not deploy it.
> All credentials are non-functional dummies.

## Setup
```
pip install -r requirements.txt
python -m app.main
```
